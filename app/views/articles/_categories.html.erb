<% @categories.each do |category| %>
  <a href="<%= category_path(category) %>" class="category" style="background-image: url(<%= url_for(category.recent.image) %>)">
    <p class="category_title"><%= category.name %></p>
    <p class="recent_title"><%= category.recent.title %></p>
  </a>
<% end %>